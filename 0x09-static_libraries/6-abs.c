#include "main.h"
/**
 * _abs - Compuyer the absolute value of an integer
 * @t: Ther integer to be computed
 * Return: The absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
